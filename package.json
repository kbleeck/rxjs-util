{
  "name": "@kbleeck/rxjs-util",
  "version": "1.0.0",
  "description": "Utility functions and classes for RxJS projects.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "files": [
    "/dist"
  ],
  "peerDependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "rxjs": "^7.8.1"
  }
}
